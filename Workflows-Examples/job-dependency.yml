name: Job Dependency

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build completed"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Deploy after build"
