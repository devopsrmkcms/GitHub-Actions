name: run bash commands
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: run bash commands
        run: |
          echo "Hello Lodu"
          ls -l
          whoami
