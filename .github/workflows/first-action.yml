name: secret demo
on: workflow_dispatch
jobs:
  secret-print:
    runs-on: ubuntu-latest
    steps:
      - name: print docker username
        run: echo "DOCKER USERNAME: ${{secret.DOCKER_USERNAME}}"






    
    

