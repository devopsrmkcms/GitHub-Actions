name: test jobs
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: list files
        run: ls -ltrh

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: testing in progress
        run: echo "Testing in prorgess"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: deploy in progress
        run: echo "deploy in progress"
    

