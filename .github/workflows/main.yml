name: Hello World

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello World
        run: |
          echo "Hello World"
          whoami
          uptime
