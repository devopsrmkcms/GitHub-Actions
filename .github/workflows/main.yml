name: Hello World
on: workflow_dispatch
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello world
        run:|
          echo "Hello World"
          whoami
          uptime
